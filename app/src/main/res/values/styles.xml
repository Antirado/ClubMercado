<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorSecondary">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyProgressBarBlue" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:progressBackgroundTint">@color/colorPrimaryGray</item>
        <item name="android:progressTint">@color/colorPrimary</item>
        <item name="android:minWidth">35dip</item>
        <item name="android:minHeight">15dp</item>
    </style>
    <style name="MyProgressBarYellow" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:progressBackgroundTint">@color/colorPrimaryGray</item>
        <item name="android:progressTint">@color/colorYellow</item>
        <item name="android:minWidth">35dip</item>
        <item name="android:minHeight">15dp</item>
    </style>
    <style name="MyProgressBarGreen" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:progressBackgroundTint">@color/colorPrimaryGray</item>
        <item name="android:progressTint">@color/colorGreen</item>
        <item name="android:minWidth">35dip</item>
        <item name="android:minHeight">15dp</item>
    </style>

</resources>
